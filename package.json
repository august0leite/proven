{
  "name": "proven-monorepo",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "yarn workspace proven-frontend dev",
    "build": "yarn workspace proven-frontend build",
    "start": "yarn workspace proven-frontend start",
    "lint": "yarn workspace proven-frontend lint"
  }
}
